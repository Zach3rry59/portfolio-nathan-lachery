export default function HeroScene({ profileKey, onSelect, onNavigate }) {
  return <section className="hero" id="home" aria-labelledby="home-title" tabIndex={-1}>
    <div className="hero-scene" aria-hidden="true"><div className="orbital"><div className="orbit orbit--one"/><div className="orbit orbit--two"/><div className="orbit orbit--three"/><div className="planet"><div className="planet-grid"/></div><span className="satellite"/><span className="orbital-cross">+</span></div></div>
    <div className="container hero-content"><div className="hero-topline"><span className="eyebrow"><span className="status-dot"/> CV & Portfolio personnel</span><span className="mono hero-coordinate">DOUAI, FRANCE</span></div><div className="hero-heading"><p className="hero-prelude">Deux univers. Une même curiosité.</p><h1 id="home-title">Nathan<br/><span className="gradient-text">Lachery<span className="title-dot">.</span></span></h1><p className="hero-subtitle">Développement d’applications<br/><span className="cross">×</span> Maintenance industrielle</p></div>
    <div className="hero-bottom"><div className="hero-caption"><span className="mono">CHOISIR UN UNIVERS</span><p>Du code aux machines,<br/>l’envie de construire.</p><span className="caption-line" aria-hidden="true"/></div><div className="profile-choices" aria-label="Choisir un profil">{[['dev','</>','01 / DEV','Développement','Applications · Web · Logiciel'],['industrie','⚙','02 / INDUSTRIE','Maintenance / Industrie','Diagnostic · Machines · Fiabilité']].map(([key,icon,label,title,description]) => <button key={key} className={`profile-choice ${key === 'dev' ? 'choice-dev' : 'choice-industry'}`} data-select={key} aria-pressed={profileKey === key} onClick={() => onSelect(key)}><span className="choice-top"><span className="choice-icon">{icon}</span><span className="mono">{label}</span></span><span className="choice-title">{title} <span aria-hidden="true">↗</span></span><span className="choice-description">{description}</span></button>)}</div></div>
    <div className="hero-baseline"><a href="#profile" onClick={e => { e.preventDefault(); onNavigate('profile'); }}><span className="scroll-line" aria-hidden="true"/>Explorer mon parcours <span aria-hidden="true">↗</span></a><span className="mono">DEUX PARCOURS, UN MÊME HORIZON</span></div></div>
  </section>;
}
